import java.util.Scanner;
public class Main {
    public static boolean allDistinct(int[] arr) {
        for (int i = 0; i < arr.length; i++) {
            for (int j = i + 1; j < arr.length; j++) {
                if (arr[i] == arr[j]) {
                    return false;
                }
            }
        }
        return true;
    }
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int[] arr = new int[n];
        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }
        if (allDistinct(arr)) {
            System.out.println("All elements in the array are distinct.");
        } else {
            System.out.println("The array contains duplicate elements.");
        }
        sc.close();
    }
}
